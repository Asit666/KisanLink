# KisanLink AI model assets

This directory is intentionally kept as a managed model store for the crop-doctor pipeline.

Expected file:
- crop_doctor_v1.pt

The project plan calls for a trained checkpoint, but the repository does not include a real trained model. The runtime service therefore starts in a safe fallback heuristic mode until a proper trained checkpoint is added.
